---
title: Tags
description: Organizing models with tags for selective generation
---


### Model: Orders

```go title="orders.dg"
model orders {
  metadata {
    tags: {
        "service": "delivery",
        "team": "Slytherin"
    }
  }

  fields {
    order_id() string
    rider_id() int
    item() string
  }

  gens {
    func order_id() {
        return UUID()
    }

    func rider_id() {
        return IntBetween(1, 500)
    }

    func item() {
        return RandomFrom("sushi", "pasta", "pizza")
    }
  }
}
```


### Model: Users

```go title="users.dg"
model users {
  metadata {
    tags: {
      "service":  "user",
      "team":  "Slytherin"
    }
  }

  fields {
    id() int
    name() string
    email() string
  }

  gens {
    func id() {
      return iter + 1
    }

    func name() {
      return Name()
    }

    func email() {
      return Email()
    }
  }
}
```


### Usage Examples

#### 1. Filter by single tag (selects orders model)

```shell
$ datagenc gen ./examples/6_metadata/tags -t service=delivery
```

**Output:**
```shell
orders{order_id:89847c16-cb80-40f6-9b44-2170de1bd765 rider_id:387 item:sushi}
```

#### 2. Filter by single tag (selects both models with team=Slytherin)

```shell
$ datagenc gen ./examples/6_metadata/tags -t team=Slytherin
```

**Output:**
```shell
orders{order_id:d19ced78-c0fc-4032-925a-32201232e87a rider_id:356 item:sushi}
users{id:1 name:Laverne Carter email:ashlyhartmann@kris.info}
```

#### 3. Filter by multiple tags (selects only users model)

```shell
$ datagenc gen ./examples/6_metadata/tags -t service=user,team=Slytherin
```

**Output:**
```shell
users{id:1 name:Catherine Kiehn email:lonniekautzer@kautzer.com}
```

#### 4. Filter by multiple tags (selects no models)

```shell
$ datagenc gen ./examples/6_metadata/tags -t service=delivery,team=user
```

**Output:**
```shell
2025-10-29T13:16:59 WARN no models found matching the specified tags: service=delivery,team=user
```
