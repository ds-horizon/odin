---
import DocsLayout from '../../../../layouts/DocsLayout.astro';

const breadcrumbs = [
  { label: 'Documentation', href: '/odin/docs/introduction/overview' },
  { label: 'Concepts', href: '/odin/docs/concepts/overview' },
  { label: 'Advanced', href: '/odin/docs/concepts/advanced/overview' },
  { label: 'Model References' },
];

const headings = [
  { depth: 2, slug: 'overview', text: 'Overview' },
  { depth: 2, slug: 'examples', text: 'Examples' },
];
---

<DocsLayout
  title="Model References"
  description="Referencing other models in datagen"
  breadcrumbs={breadcrumbs}
  headings={headings}
>
  <h2 id="overview">Overview</h2>

  <p>You can reference other models to create related data:</p>

  <pre set:html={`<code>model User {
  fields {
    id() int
    name() string
  }

  gens {
    func id() {
      return iter + 1
    }

    func name() {
      return Name()
    }
  }
}

model Order {
  fields {
    id() int
    userId() int
    amount() float
  }

  gens {
    func id() {
      return iter + 1
    }

    func userId() {
      return Reference("User", "id")
    }

    func amount() {
      return FloatBetween(10.0, 1000.0)
    }
  }
}</code>`}></pre>

  <h2 id="examples">Examples</h2>

  <p>
    See <a href="/odin/docs/examples/5_reference/reference">Reference Examples</a> for detailed usage.
  </p>

  <h2>Related Documentation</h2>
  <ul>
    <li><a href="/odin/docs/examples/5_reference/reference">Reference Examples</a></li>
  </ul>
</DocsLayout>

