---
import DocsLayout from '../../../../layouts/DocsLayout.astro';

const breadcrumbs = [
  { label: 'Documentation', href: '/odin/docs/introduction/overview' },
  { label: 'Concepts', href: '/odin/docs/concepts/overview' },
  { label: 'Advanced', href: '/odin/docs/concepts/advanced/overview' },
  { label: 'Optional Sections' },
];

const headings = [
  { depth: 2, slug: 'overview', text: 'Overview' },
  { depth: 2, slug: 'examples', text: 'Examples' },
];
---

<DocsLayout
  title="Optional Sections"
  description="Making fields optional in datagen models"
  breadcrumbs={breadcrumbs}
  headings={headings}
>
  <h2 id="overview">Overview</h2>

  <p>Make fields optional using conditional logic:</p>

  <pre set:html={`<code>gens {
  func middleName() {
    if Bool() {
      return Name()
    } else {
      return null
    }
  }

  func phoneNumber() {
    // 70% chance of having a phone number
    if IntBetween(1, 100) <= 70 {
      return Phone()
    } else {
      return null
    }
  }
}</code>`}></pre>

  <h2 id="examples">Examples</h2>

  <h3>Optional Email</h3>

  <pre set:html={`<code>func secondaryEmail() {
  if Bool() {
    return Email()
  }
  return null
}</code>`}></pre>

  <h2>Related Documentation</h2>
  <ul>
    <li><a href="/odin/docs/language/dsl-specification">DSL Specification</a></li>
    <li><a href="/odin/docs/concepts/advanced/function-calls">Function Calls</a></li>
  </ul>
</DocsLayout>

