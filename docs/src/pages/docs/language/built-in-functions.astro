---
import DocsLayout from '../../../layouts/DocsLayout.astro';

const breadcrumbs = [
  { label: 'Documentation', href: '/odin/docs/introduction/overview' },
  { label: 'Language' },
  { label: 'Built-in Functions' },
];

const headings = [
  { depth: 2, slug: 'overview', text: 'Overview' },
  { depth: 2, slug: 'names', text: 'Names' },
  { depth: 2, slug: 'numbers', text: 'Numbers' },
  { depth: 2, slug: 'internet', text: 'Internet' },
  { depth: 2, slug: 'dates', text: 'Dates' },
  { depth: 2, slug: 'text', text: 'Text' },
];
---

<DocsLayout
  title="Built-in Functions"
  description="Complete reference of datagen built-in generator functions"
  breadcrumbs={breadcrumbs}
  headings={headings}
>
  <h2 id="overview">Overview</h2>

  <p>
    datagen provides a rich set of built-in functions for generating realistic data.
  </p>

  <h2 id="names">Names</h2>

  <table>
    <thead>
      <tr>
        <th>Function</th>
        <th>Returns</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>Name()</code></td>
        <td>Full name</td>
        <td>"John Smith"</td>
      </tr>
      <tr>
        <td><code>FirstName()</code></td>
        <td>First name</td>
        <td>"John"</td>
      </tr>
      <tr>
        <td><code>LastName()</code></td>
        <td>Last name</td>
        <td>"Smith"</td>
      </tr>
    </tbody>
  </table>

  <h2 id="numbers">Numbers</h2>

  <table>
    <thead>
      <tr>
        <th>Function</th>
        <th>Returns</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>IntBetween(min, max)</code></td>
        <td>Random integer</td>
        <td>42</td>
      </tr>
      <tr>
        <td><code>FloatBetween(min, max)</code></td>
        <td>Random float</td>
        <td>3.14159</td>
      </tr>
      <tr>
        <td><code>Bool()</code></td>
        <td>Random boolean</td>
        <td>true</td>
      </tr>
    </tbody>
  </table>

  <h2 id="internet">Internet</h2>

  <table>
    <thead>
      <tr>
        <th>Function</th>
        <th>Returns</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>Email()</code></td>
        <td>Email address</td>
        <td>"john@example.com"</td>
      </tr>
      <tr>
        <td><code>URL()</code></td>
        <td>URL</td>
        <td>"https://example.com"</td>
      </tr>
      <tr>
        <td><code>IPv4()</code></td>
        <td>IPv4 address</td>
        <td>"192.168.1.1"</td>
      </tr>
      <tr>
        <td><code>Domain()</code></td>
        <td>Domain name</td>
        <td>"example.com"</td>
      </tr>
    </tbody>
  </table>

  <h2 id="dates">Dates</h2>

  <table>
    <thead>
      <tr>
        <th>Function</th>
        <th>Returns</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>Datetime()</code></td>
        <td>Current datetime</td>
        <td>"2024-01-15T10:30:00Z"</td>
      </tr>
      <tr>
        <td><code>DateBetween(start, end)</code></td>
        <td>Date in range</td>
        <td>"2023-06-15"</td>
      </tr>
    </tbody>
  </table>

  <h2 id="text">Text</h2>

  <table>
    <thead>
      <tr>
        <th>Function</th>
        <th>Returns</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>Sentence()</code></td>
        <td>Random sentence</td>
        <td>"The quick brown fox."</td>
      </tr>
      <tr>
        <td><code>Paragraph()</code></td>
        <td>Random paragraph</td>
        <td>"Lorem ipsum..."</td>
      </tr>
      <tr>
        <td><code>Word()</code></td>
        <td>Random word</td>
        <td>"example"</td>
      </tr>
    </tbody>
  </table>

  <h2>Usage Example</h2>

  <pre set:html={`<code>model User {
  fields {
    name() string
    email() string
    age() int
  }

  gens {
    func name() {
      return Name()
    }

    func email() {
      return Email()
    }

    func age() {
      return IntBetween(18, 65)
    }
  }
}</code>`}></pre>

  <h2>Related Documentation</h2>
  <ul>
    <li><a href="/odin/docs/language/dsl-specification">DSL Specification</a></li>
    <li><a href="/odin/docs/concepts/data-model">Data Model Concepts</a></li>
  </ul>
</DocsLayout>

