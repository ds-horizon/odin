---
import DocsLayout from '../../../../layouts/DocsLayout.astro';

const breadcrumbs = [
  { label: 'Documentation', href: '/odin/docs/introduction/overview' },
  { label: 'Examples' },
  { label: 'Metadata Overview' },
];

const headings = [
  { depth: 2, slug: 'overview', text: 'Overview' },
  { depth: 2, slug: 'examples', text: 'Examples' },
];
---

<DocsLayout
  title="Metadata Overview"
  description="Using metadata in datagen models"
  breadcrumbs={breadcrumbs}
  headings={headings}
>
  <h2 id="overview">Overview</h2>

  <p>Metadata provides additional configuration for your models:</p>

  <pre set:html={`<code>model User {
  metadata {
    count = 1000
    tags = ["users", "test"]
  }

  fields {
    name() string
  }

  gens {
    func name() {
      return Name()
    }
  }
}</code>`}></pre>

  <h2 id="examples">Examples</h2>

  <ul>
    <li><a href="/odin/docs/examples/6_metadata/count/count">Count</a> - Setting record count</li>
    <li><a href="/odin/docs/examples/6_metadata/tags/tags">Tags</a> - Organizing with tags</li>
  </ul>
</DocsLayout>

