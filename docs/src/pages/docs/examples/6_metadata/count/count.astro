---
import DocsLayout from '../../../../../layouts/DocsLayout.astro';

const breadcrumbs = [
  { label: 'Documentation', href: '/odin/docs/introduction/overview' },
  { label: 'Examples', href: '/odin/docs/examples/6_metadata/metadata-overview' },
  { label: 'Count' },
];

const headings = [
  { depth: 2, slug: 'example', text: 'Example' },
];
---

<DocsLayout
  title="Count Metadata Example"
  description="Setting default record count in metadata"
  breadcrumbs={breadcrumbs}
  headings={headings}
>
  <h2 id="example">Example</h2>

  <pre set:html={`<code>model User {
  metadata {
    count = 5000
  }

  fields {
    id() int
    name() string
  }

  gens {
    func id() {
      return iter + 1
    }

    func name() {
      return Name()
    }
  }
}</code>`}></pre>

  <p>When you run <code>datagenc gen User.dg</code> without <code>--count</code>, it generates 5000 records by default.</p>

  <h2>Related Documentation</h2>
  <ul>
    <li><a href="/odin/docs/examples/6_metadata/metadata-overview">Metadata Overview</a></li>
  </ul>
</DocsLayout>

